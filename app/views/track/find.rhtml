<h3>Search Results</h3>
<p><%= h(@message) if @books.empty? %></p>
<ul>
<% @books.each do |book| %>
	<li><%= link_to book.name, {:controller => 'item', :action => 'show', :id => book.tbid} %></li>
<% end %>
</ul>
