<li><%= link_to( 
			image_tag( "#{friend.main_photo.thumb_url(80)}", :class => "main_photo"),
	 		{ :action => 'show', 
		      :controller => 'user', 
		      :id => friend.id} ) %><br />
		<%= link_to friend.display_name, {:action => 'show', :controller => 'user', :id => friend.id}, {:class => 'friend_caption'}%>
</li>
